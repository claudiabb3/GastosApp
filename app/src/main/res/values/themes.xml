<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GastosApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="CustomSpinner" parent="Widget.AppCompat.Spinner">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">20sp</item>
    </style>
    <style name="Theme.GastosApp" parent="Base.Theme.GastosApp" />


    <style name="CustomSpinnerStyle" parent="Widget.AppCompat.Spinner">
        <item name="android:background">@drawable/borde</item> <!-- Fondo con borde -->
        <item name="android:popupBackground">@drawable/borde</item> <!-- Fondo desplegable con borde -->
        <item name="android:textColor">@color/white</item>
    </style>
</resources>